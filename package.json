{
  "name": "pgls-vscode",
  "displayName": "pgls",
  "description": "VSCode client for the pgls PostgreSQL language server.",
  "version": "0.3.0",
  "publisher": "winebarrel",
  "license": "MIT",
  "author": {
    "name": "Genki Sugawara",
    "email": "sugawara@winebarrel.jp"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/winebarrel/pgls-vscode.git"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "icon": "etc/icon.png",
  "main": "./out/extension.js",
  "activationEvents": [
    "workspaceContains:.pgls.json"
  ],
  "contributes": {
    "configuration": {
      "title": "pgls",
      "properties": {
        "pgls.command": {
          "type": "string",
          "default": "pgls",
          "description": "Path to the pgls executable. Must be on $PATH or absolute."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p .",
    "watch": "tsc -p . -w",
    "lint": "eslint src",
    "package": "npm ci && vsce package",
    "publish": "npm ci && vsce publish"
  },
  "dependencies": {
    "vscode-languageclient": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.75.0",
    "@typescript-eslint/eslint-plugin": "^8.59.2",
    "@typescript-eslint/parser": "^8.59.2",
    "@vscode/vsce": "^3.0.0",
    "eslint": "^10.3.0",
    "typescript": "^6.0.0"
  }
}
